package test

import (
	"testing"
)

func TestGetReportOK(t *testing.T) { //TODO
	/*ctl := gomock.NewController(t)
	defer ctl.Finish()

	db := mock_repository.NewMockDatabase(ctl)
	giteaApi := mock_gitea.NewMockGiteaInterface(ctl)*/
}
